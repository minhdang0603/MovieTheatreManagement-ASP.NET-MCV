@model List<Director>

@{
	ViewData["Title"] = "Index";
}

<div class="container mt-4">
	<h1 class="text-center">Director List</h1>

	<div class="d-flex justify-content-between mb-3">
		<a asp-action="upsert" class="btn btn-success">
			<i class="bi bi-plus-circle"></i> Create New
		</a>
	</div>

	<table class="table table-striped table-hover">
		<thead class="table-dark">
			<tr>
				<th class="text-center">
					Director Name
				</th>
				<th class="text-center">Actions</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<td class="text-center">
						@item.DirectorName
					</td>
					<td class="text-center">
						<div class="btn-group">
							<a asp-action="upsert" asp-controller="directors" asp-route-id="@item.DirectorId" class="btn btn-warning">
								<i class="bi bi-pencil-square"></i> Edit
							</a>
							<a onclick="Delete('/admin/directors/delete/@item.DirectorId')" class="btn btn-danger" style="cursor: pointer">
								<i class="bi bi-trash"></i> Delete
							</a>
						</div>
					</td>
				</tr>
			}
		</tbody>
	</table>
</div>

@section Scripts
{
	<script src="~/js/deletedConfirmation.js"></script>
}
